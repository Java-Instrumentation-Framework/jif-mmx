/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package edu.mbl.cdp.ps.orient;

/**
 *
 * @author GBH
 */
public class DeltaAnalyzer {
 
   // Compare two measurements (ztPoints), within one dataset, or between two datasets.
   // int deltaTimePoints... between measurements
   // Generate difference images (signed float[]) of anisotropy and orientation.
   // Map pos/neg values to posColor (blue) / negColor (red)
   // 
   
}
